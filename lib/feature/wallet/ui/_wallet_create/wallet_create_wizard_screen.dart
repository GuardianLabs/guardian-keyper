import 'package:guardian_keyper/ui/widgets/common.dart';

class WalletCreateWizardScreen extends StatelessWidget {
  static const route = '/wallet/create/wizard';

  const WalletCreateWizardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
